if b>c 
.text
	bge s2,s1, else #c>=b
	d=b-a
else:
	e=c+a
	
if (g>h) && ( i==j)

.text
	bge h,g, else
	bne i,j,else
	f=f+g
	beq x0,x0, exit
else:
	f=f-g
exit:
.text
	blt h,g,then
	beq i,j,then
	f=f-g
then:
	f=f+g

	
	
